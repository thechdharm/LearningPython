class Node:
    def __init__(data):
        self.data = data
        self.next = None





if __name__ == "__main__":
    number_of_nodes = input("Enter Number of Nodes:")
    head = None
    
    print("Now enter nodes data one by one:")
    for _ in range(number_of_nodes):
        node_data = input("Enter Data of Node:")
        if head is None:
            head = Node(node_data)
            continue
        head.ne
        






    
